<%= form_for [@conversation, @conversation.messages.new] do |f| %>
  <div class="form-group">
    <%= f.trix_editor :content, placeholder: "Write your reply" %>
  </div>

  <div class="form-group">
    <%= f.submit "Send message", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
