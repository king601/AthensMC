<%= form_for [@forum_thread, @forum_post] do |f| %>
  <div class="form-group">
    <%= f.text_area :body, placeholder: "Write your reply", rows: 10, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit "Add Reply", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
